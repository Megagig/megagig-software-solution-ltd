# progress-tracker.md — Megagig Website Build Progress

> **Living document.** Checkboxes mirror `build-plan.md` exactly, phase for phase, feature for feature. Auto-updated after each feature ships (check the box the moment it's done and verified, not before). A new session should be able to read only this file and `build-plan.md` and know precisely where the project stands — no scrollback required.

**Status key:** `[ ]` not started · `[~]` in progress · `[x]` done & verified

---

## Phase 0 — Scaffold & infrastructure
- [x] `grit new megagig-site --triple --next` run
- [x] `.env` configured from `.env.example`
- [x] `docker compose up -d` verified healthy — Postgres (`healthy`), Redis (`healthy`), MinIO (`/minio/health/live` → 200), Mailhog (UI → 200)
- [x] Base `User`/auth model confirmed — matches `architecture.md` §6 (`admin` role sufficient for v1). `apps/api/internal/models/user.go` carries the full Grit "kitchen sink" scaffold (`RoleAdmin`/`RoleEditor`/`RoleUser`, Session/2FA/SSO/SAML/tickets/backups/feature-flags/webhooks/GDPR/access-reviews) — left in place, dormant, toggleable via `MODULE_*` env flags per explicit user decision: no deletions without approval. Nothing here blocks or conflicts with `project-requirements.md`'s single-admin-role v1 scope.
- [x] `ui-tokens.md` values wired into `packages/shared/themes` — `packages/shared/themes/tokens.css`, Tailwind v4 CSS-first `@theme`, imported by both `apps/web` and `apps/admin` (single source of truth). Both apps upgraded `tailwindcss`/`postcss` config to v4 (`@tailwindcss/postcss`) as part of this. Admin's prior Grit boilerplate multi-theme system (`[data-theme]` atlas/aurora/pulse/midnight, doubled-up classes like `bg-bg-secondary`) was fully renamed across 98 files to the ui-tokens.md-named utilities (`bg-brand`, `bg-surface`, `text-foreground-muted`, etc.); admin's separate `(auth)` login-page theme engine (`packages/shared/themes.ts`) is untouched/out of scope.
- [x] Global fonts + base Tailwind config set — Inter + JetBrains Mono loaded via `next/font/google` in both `apps/web/app/layout.tsx` and `apps/admin/app/layout.tsx`. Fixed a wiring bug found during this verification pass: Inter's `next/font` variable was named `--font-display` (unused anywhere), so the `font-sans` utility was silently falling back to `tokens.css`'s static system-font list instead of rendering the actual self-hosted Inter — renamed to `--font-sans` in both layouts to match how JetBrains Mono was already correctly wired to `--font-mono`.
- [x] Dev servers verified: API `:8080` (`/api/health` → 200, `/studio` → 200), Web `:3000` (→ 200), Admin `:3001` (→ 200) — all three already running from a prior session; confirmed live rather than restarted.

## Phase 1 — Core content resources
- [x] Upload — confirmed already present from base scaffold, unchanged
- [x] TeamMember resource generated + verified (`grit generate resource`)
- [x] Testimonial resource generated + verified — `case_study_id` made nullable (optional) to match `architecture.md`'s "CaseStudy, nullable" spec; generated `binding:"required"` removed
- [x] CaseStudy resource generated + verified — `category_tags`/`tech_stack` typed `string_array`; nullable `testimonial_id` (belongs_to Testimonial) added by hand as a follow-up field (Grit has no CLI for adding a field to an existing resource — 5-layer manual edit: model, schema, type, admin resource, per the grit skill's documented pattern)
- [x] Product resource generated + verified — `feature_bullets` typed `string_array`; `screenshots:many_to_many:Upload` added (architecture.md calls for multiple screenshots; build-plan.md's literal generate command omitted any image field entirely)
- [x] JobOpening resource generated + verified
- [x] FAQ resource generated + verified
- [x] BlogPost resource generated + verified — extended the **existing** `Blog` resource (already shipped pre-Phase-1: model/service/handler/admin, own hand-rolled `/api/admin/blogs` endpoint) rather than generating a parallel resource. Added `author_id` (belongs_to TeamMember), `tags` (string_array), `seo_title`, `seo_description` across model + handler (hand-rolled request DTOs, not auto-bound) + shared schema/type + admin resource. Kept the existing `Image` (string URL) and `Content` field names as-is rather than converting to `cover_image_id`/`Body` — avoids reworking a working feature for a naming-only gain; noted here as the deliberate divergence from architecture.md's exact field names.
- [x] Lead resource generated + verified — status/source/project_type/budget_range left as plain strings (matches the existing `User.Role` convention: app-level Go constants, not DB enums). Status badge coloring is explicitly Phase 2 scope, not done here.
- [x] SiteSettings singleton hand-built + verified — model, service (get-or-create), handler, public `GET /api/v1/site-settings` + admin-only `PUT`, shared schema/type, dedicated non-DataTable admin page at `/site-settings` (added to sidebar nav, admin-gated)
- [x] Seed data added — admin user/blogs already existed and were skipped (idempotent); added `SeedSiteSettings` (placeholder contact info/hero copy/pricing blurbs) and `SeedDemoContent` (2 case studies, 2 products, 1 testimonial — PharmacyCopilot/BusinessCopilot placeholders, real content is Phase 5). Verified via `go run ./cmd/seed`.
- [x] End-to-end verification — Go API builds clean; `apps/admin` and `apps/web` both `tsc --noEmit` clean; all 7 new protected list endpoints return 401 (unauthenticated) confirming registration; public `/api/v1/site-settings` returns seeded data; all 9 relevant admin pages return 200

**Known gaps carried forward (not blocking Phase 1, tracked for later phases):**
- No public/published-only read routes exist yet for CaseStudy, Product, Testimonial, TeamMember, FAQ (only Blog has one, hand-built pre-Phase-1). `grit generate resource` never creates these — Phase 4 (when `apps/web` pages actually need to fetch this content) must hand-add a `ListPublished`/`GetBySlug`-style public route + service method per resource, mirroring Blog's existing pattern, per `architecture.md` rule #8.
- ~~Generated services never call `.Preload()`~~ — **corrected in Phase 2**: this was wrong, based on inspecting unused `internal/services/*.go` files. The actual generated `List` handlers (`paginate.List[T]` pattern) do Preload correctly. See Phase 2 notes below.
- Found and fixed a systemic admin-resource generation bug while verifying: every `belongs_to:Upload` relationship (`hero_image`, `photo`, `avatar`, `screenshots`) referenced a `.name` display field that doesn't exist on the `Upload` model (it's `original_name`) — fixed across TeamMember, CaseStudy, Testimonial, Product.
- Added a new `"tags"` admin field/column type (`apps/admin/lib/resource.ts`, `TagsField` component, `form-builder.tsx`, `cell-renderers.tsx`) since no free-text array/tag input existed — the generator had been mapping every `string_array` field to `type: "images"` (an actual image-upload dropzone), which would have made `category_tags`/`tech_stack`/`feature_bullets`/`tags` fields unusable. Log this in `ui-registry.md`.
- Set `DisableForeignKeyConstraintWhenMigrating: true` in `apps/api/internal/database/database.go` — the genuine two-way `CaseStudy` ↔ `Testimonial` belongs_to relation broke GORM's AutoMigrate ordering ("relation does not exist"). Referential integrity is already enforced at the service layer for every model, so this wasn't previously load-bearing.

**`/review` pass (2026-09-17) found and fixed 4 real issues** — the manual "5-layer" field addition for `CaseStudy.testimonial_id` was incomplete when first done; caught by a dedicated review before Phase 3, not caught by the build/type-check/smoke-test verification used throughout Phase 1 (those checks confirm the code runs, not that every field is wired end-to-end):
1. **[Critical, fixed]** `CaseStudy.testimonial_id` was a dead field — `Create`/`Update`/`Patch` handlers never read it from the request body, and no handler method ever `Preload("Testimonial")`d it. Fixed all three write paths + added `Preload("Testimonial")` to `List`/`GetByID`/`Create`/`Update`/`Patch`. Verified live: set via `PUT`, confirmed both `testimonial_id` and the nested `testimonial` object round-trip correctly.
2. **[Important, fixed]** `Testimonial.Create` still had `binding:"required"` on `case_study_id` *and* `avatar_id` in the handler's request struct, independent of the model/schema/form all being marked optional — creating a "general" testimonial not tied to a case study or avatar failed with a 422. Removed both `required` tags; also fixed the shared Zod schema, TS type, and admin form field for `avatar_id` (same nullable spec in `architecture.md`, previously only `case_study_id` had been addressed). Verified live: testimonial now creates successfully with both fields omitted.
3. **[Important, fixed]** `Blog.Author` was never `Preload()`ed anywhere in `blog_service.go` (`List`, `ListPublished`, `GetByID`, `GetBySlug`, `Update`) — `author_id` saved correctly but the nested author object (used by the admin table's `author.name` column) was always empty. Added `Preload("Author")` to all five methods.
4. **[Minor, fixed]** `--color-status-*` tokens (used by the new `LeadStatusBadge`) were only defined in `:root`, never overridden for `.dark` — added dark-mode values to both `tokens.css` and `ui-tokens.md`, following the same brightening pattern as the other semantic colors (success/warning/danger/info).

## Phase 2 — Admin panel polish
- [x] Dashboard stats/chart/activity widgets built — added a "Megagig overview" row (New leads this week, Published case studies, Published products) above the existing generic per-resource widgets. The generic "By resource" section (pre-existing, `ResourceStatCard`/`ResourceLatestTable`) already gives every new resource a free Total + 30-day sparkline + Latest-N once registered in `resource_stats_dispatch.go` (auto-injected by `grit generate`) — satisfies the "ChartWidget (leads over 30 days)" and "ActivityWidget (latest 10 leads)" asks without new bespoke widgets. Only the 3 site-specific numbers (7-day window, published-only filters) needed hand-building, reusing existing `paginate` package query params (`?created_since=7d`, `?published=true`) — zero new endpoints.
- [x] Leads resource UI polish — status badge coloring (`LeadStatusBadge`, using `--color-status-*` tokens 1:1 per ui-rules.md §13), status filter dropdown (table + form), status field upgraded from free-text to a `select` (prevents a typo silently breaking the badge/filter mapping). Internal-notes textarea was already present from Phase 1 generation, no change needed.
- [x] Admin routes confirmed behind auth — `AdminLayout` (`components/layout/admin-layout.tsx`) already redirects to `/login` on `isError` or no user; pre-existing, verified not rebuilt.
- [x] Admin subdomain + `robots.txt` disallow configured — added `apps/admin/app/robots.ts` (Next.js Metadata API route, disallow-all). Actual subdomain DNS/hosting is Phase 8 deployment work, correctly out of scope here.

**Backend fixes made in support of the above (not resource-specific, so noted here):**
- `CaseStudy`/`Product` list handlers didn't support `?published=true` filtering at all (the admin table's own "Published" filter checkbox — generated in Phase 1 — was silently non-functional server-side). Added `.With("published", ...)` to both.
- `Lead` list handler had no status filter support. Added `.With("status", c.Query("status"))`.
- **Correction to a Phase 1 note**: progress-tracker previously claimed "generated services never call `.Preload()` on belongs_to relations." That was based on inspecting the unused `internal/services/*.go` files. The actual generated `List` handlers (`paginate.List[T]` pattern) *do* call `.Preload()` correctly (confirmed: `CaseStudy` preloads `HeroImage`, `Product` preloads `Screenshots`) — the `internal/services/case_study.go`-style files are dead code from an older/unused code path, not what's actually wired in `routes.go`. No cross-cutting Preload gap exists; retracting that item.

## Phase 3 — Public site: global chrome
- [x] `(marketing)` navbar + footer layout built — retrofitted `apps/web` into a literal `app/(marketing)/` route group per `architecture.md` §4 (previously flat `app/page.tsx` + `app/blog/`, chrome applied via a pathname-check `AppChrome.tsx`); moved `page.tsx`/`blog/` in, deleted `AppChrome.tsx`, chrome now lives in `(marketing)/layout.tsx`. Also deleted the unused `app/(auth)/` route group (Grit's default customer-account scaffold — confirmed out of scope, no public visitor accounts, per `project-requirements.md` §7).
- [x] WhatsApp floating action button built — `WhatsAppFab`, number sourced from `SiteSettings.whatsapp_number` via a server-side fetch in `(marketing)/layout.tsx` (plain `fetch`, not the client-only axios instance in `lib/api.ts`). Renders nothing if the fetch fails or the number is unset.
- [x] Shared UI primitives built (Button, Card, Badge, SectionHeading, Accordion, Carousel, StatCallout, TechIcon/TechStackStrip) — all in `apps/web/components/ui/`, logged in `ui-registry.md` with full class-level detail via `/imprint`. Hand-built with the already-installed CVA/clsx/tailwind-merge stack rather than the shadcn CLI `library-docs.md` calls for — see the note in `ui-registry.md` for why (token vocabulary mismatch makes the CLI's output require a full rewrite anyway). Installed `@radix-ui/react-accordion` as the one genuinely-worth-it new dependency, for real keyboard/ARIA accordion behavior.
- [x] Dark/light theme toggle verified end-to-end — `ThemeToggle` (mirrors `apps/admin`'s `DarkModeToggle` pattern, duplicated rather than shared per code-standards.md §3), flips `.dark` on `<html>`, persists to `localStorage`. Verified live against the running dev server: navbar/footer/FAB render real seeded `SiteSettings` data (WhatsApp number, contact email/phone, social links), `/blog` and `/` correctly get chrome, `/forms/[token]` correctly stays chromeless, `/services` etc. correctly 404 (Phase 4 not started yet). `tsc --noEmit` and `next build` both clean.

**Rebuilt from scratch (not patched):** the pre-existing `navbar.tsx`/`footer.tsx`/root `layout.tsx` metadata were 100% unmigrated Grit demo scaffold — "Built with Grit" copy, links to Grit's docs/GitHub, an "Admin" nav link, and CSS classes (`text-text-secondary`, `bg-bg-tertiary`) that don't exist in `ui-tokens.md`'s token set at all. Full rebuild against `ui-rules.md` §3, not a diff.

**Known gap carried forward (not blocking Phase 3):** `apps/web/app/page.tsx`'s Home content (now `(marketing)/page.tsx`) is untouched Grit demo content (framework marketing copy, `usePublicBlogs` recent-posts section) — moving it into the new route group didn't fix its content, since rebuilding Home itself is explicitly Phase 4.1 scope, not Phase 3 chrome work. Root `not-found.tsx`/`error.tsx` also still use pre-existing raw shadcn-default classes (`text-primary`, `bg-red-500/10`) not in `ui-tokens.md` — pre-existing debt, untouched here since it's outside chrome/primitives scope; worth a pass whenever those files are next touched.

## Phase 4 — Public site: pages

**Demo `CaseStudy` seed data expanded (same day, real products):** the founder confirmed 5 more real, shipped Megagig products with matching real screenshots already in `apps/web/public/` — ACPN Ota Zone, CCRN OAU Alumni, MegaPro ERP, YazzyOS, SocietyLedger — added as proper `CaseStudy` seed rows (`internal/database/demo_content_seeder.go`) alongside the original PharmacyCopilot/BusinessCopilot pair, not hardcoded into any component (CaseStudy is an admin-managed resource per code-standards.md §6). Also confirmed but **not added — no real screenshot exists for these**: ACPN Kwara State Portal, MedSafe, FLOWMINT, and one unnamed URL (kaneopromovers.com). Seeding itself was changed from a blanket "skip everything if any CaseStudy row exists" guard to per-slug `FirstOrCreate` idempotency — the old guard meant a database that already had the original 2 rows could never pick up newly-added seed entries without a manual wipe; the same idempotency was applied to the `Product`/`Testimonial` seed loops below it, since removing the blanket guard meant those now also re-run on every seed invocation (they weren't idempotent before because they never needed to be).

**Phase 4.0 (backend prerequisite, added ahead of schedule):** `CaseStudy`, `Product`, `Testimonial`, and `FAQ` only had protected (JWT-required) routes — no anonymous visitor could ever have fetched this content, a hard blocker discovered while architecting Home. Added public, published-only routes mirroring `Blog`'s existing `ListPublished`/`GetBySlug` pattern, under a new `v1.Group("/public")` (not the bare resource path — that's already taken by the protected CRUD, unlike Blog's, which freed it by moving admin CRUD to `/admin/blogs/*`): `GET /public/case-studies` (+ `/:slug`), `GET /public/products` (+ `/:slug`), `GET /public/testimonials`, `GET /public/faqs`. `TeamMember`'s public route deferred to the `/team` page. All four verified live (real seeded data returns, unauthenticated; bad slugs 404; existing protected routes still 401 as before — no regression).

- [x] Home (`/`) — rebuilt from scratch, replacing 100% Grit demo content. All ~14 sections from `project-requirements.md` §5.1 built from Phase 3 primitives: Hero, ShowcaseStrip (real product screenshots from `apps/web/public/`), CaseStudyPreview, TechStack, ServicesGrid (8 static services), ProductsSection, SelectedWork (`#selected-work` anchor), PricingTeaser, QuoteCtaBand, TestimonialsCarousel, FounderSpotlight, OurStory, FaqAccordion (renders nothing — no FAQs seeded yet, Phase 5), ContactBlock (`LeadForm`), ClosingCta. All colocated under `(marketing)/_components/` per code-standards.md §3 (not yet reused by a second page). Real founder/stats content confirmed directly by the user — see Decisions below. Verified live against the running dev API with real seed data (screenshot images 200, founder photo 200, 8 service links present, FAQ section correctly absent, closing-CTA stats present); `tsc --noEmit`, `next build`, and the full 34-test suite all clean.

**Visual redesign pass (same day, post-`/recover`):** the first Home build was token/rule-compliant but visually flat compared to the reference site (desishub.com) — diagnosed via `/recover` as Failure Mode 3 (the approach, not a patchable bug: literal token/rule compliance isn't the same as matching the reference's visual craft). Went through `/architect` again for the redesign specifically. Changes:
- Fixed a real bug: `CaseStudyCard`'s category tags were independently `absolute`-positioned and rendered on top of each other with 2+ tags — now a `flex gap-2` row.
- Added `BrowserFrame` (new primitive, `apps/web/components/ui/browser-frame.tsx`) — fake browser-chrome wrapper (traffic lights + URL bar) around every real screenshot, replacing plain bordered boxes and letter-placeholder avatars. Backed by a new slug→screenshot lookup (`lib/product-screenshots.ts`) since demo `CaseStudy`/`Product` records have no real `Upload`-backed hero image yet.
- Rebuilt `Hero`: bigger/bolder type scale (up to `text-7xl`), tighter leading, and a subtle radial-glow background — required updating `ui-rules.md` §1's "no cartoonish gradients" line to explicitly distinguish a restrained low-opacity brand-color glow from the loud decorative gradients the rule actually guards against, rather than quietly drifting from a documented rule.
- Added `FeaturedProjects` (new section) — a dark panel reusing the `Carousel` primitive (not a bespoke tab-selector, per the redesign decision) showing real case studies with real screenshots. `SectionHeading` gained an `invert` prop for this hardcoded-dark-panel case, replacing an earlier fragile arbitrary-child-selector attempt.
- `ServicesGrid`/`PricingTeaser`: one card each ("AI Automation") now gets a solid `bg-accent` fill to break up the grid, matching the reference's own highlighted-card technique with our own token.
- Known simplification, disclosed not hidden: the Hero's mixed-color word emphasis only applies to the hardcoded fallback headline — `SiteSettings.hero_headline` already has real seeded content, so the live site renders it big/bold but monochrome. A substring-matching hack to color-split arbitrary admin-provided text was deliberately not built.
- Re-verified live end-to-end after the redesign; `tsc --noEmit`, `next build`, and the full 34-test suite all clean.

**Hero device showcase (same day, follow-up request with a reference image):** added `HeroShowcase` — a tilted, overlapping 3-device screenshot composition directly under the hero CTAs, matching an image the user provided. Desktop-only (the layered/rotated composition doesn't hold up at mobile width). This made the separate `ShowcaseStrip` carousel section (a single rotating screenshot, previously right below Hero) redundant — Hero now carries its own strong device-showcase moment — so `ShowcaseStrip` was deleted rather than left competing with it. Re-verified live (`tsc --noEmit`, `next build`, full 34-test suite all clean).
**Home polish pass (2026-09-18, same day as the CaseStudyPreview `/recover`):**
- `TechStack`: swapped every generic Lucide stand-in for a real brand logo the user added to `apps/web/public/` (Next.js, React, Node.js, Go, a combined MongoDB/PostgreSQL mark, Electron, Expo, Wails) — Tailwind still has no logo file, so it keeps its Lucide `Palette` fallback rather than fabricating a mark. Logos render via `next/image` with `unoptimized` — these source files are already tiny (1–7KB), and routing them through Next's resize/recompress pipeline for a ~40px display size introduced visible blur; serving the original bytes and letting the browser downscale reads sharp.
- `ServicesGrid`: redesigned per the user's "professional/visually appealing/beautiful" request — icons now sit in a tinted rounded-square badge (`bg-brand/10`, `bg-white/15` on the highlighted card), an `ArrowUpRight` fades in bottom-right on hover, added a section subhead, bumped padding/gap. Added a 9th service, **Accounting Software Automations** (`Calculator` icon), at the user's request specifically to make the 3-column grid divide evenly (8 items left an unbalanced 2-card last row; 9 fills exactly 3 full rows).
- `ProductsSection`: screenshot frame now uses the same `bezel="thick"` dark `BrowserFrame` + hover-lift/zoom treatment as `CaseStudyPreview`, per the user's explicit "make it look like the case studies section" request. Added the **MedSafe** screenshot (`lib/product-screenshots.ts`) once the user supplied it.
- `CaseStudyCard` (used only by `SelectedWork` — `CaseStudyPreview` has its own bespoke layout now): the user first asked for this to also match `CaseStudyPreview`'s dark `BrowserFrame` treatment, then **redirected mid-task** to instead match `HeroShowcase`'s tilted, gradient-ring `TabletFrame` look exactly (a provided reference image) — implemented that instead: alternating `-rotate-2`/`rotate-2` tilt + alternating `brand`/`accent` ring per card index (mirrors Hero's own alternating pattern), straightening to `rotate-0` on hover. Added an 8th case study, **Kaneo Pro Movers** (`kaneopromovers.com` — a real prior Megagig client, previously noted in this file as "confirmed but not added, no screenshot" until the user supplied one), specifically to balance `SelectedWork`'s `lg:col-span-2`-first grid to exactly 3 full rows (7 items left a 2-card last row; 8 divides evenly against the spanning first card).
- Replaced `YazzyosAccount.PNG` in place with a brighter version the user provided — no code change needed, the existing `product-screenshots.ts` mapping already pointed at that filename.
- Re-verified after every step in this pass: `tsc --noEmit`, `next build`, and the full 35-test suite all clean; each content change confirmed live via curl against the running dev server (not just "should work").

- [ ] Services index + detail (`/services`, `/services/[slug]`)
- [ ] Products index + detail (`/products`, `/product/[slug]`)
- [ ] Case studies index + detail (`/case-studies`, `/case-study/[slug]`)
- [ ] Pricing (`/pricing`)
- [ ] Start a project (`/start-project`)
- [ ] Contact (`/contact-us`)
- [ ] About (`/about-us`)
- [ ] Team (`/team`)
- [ ] Careers (`/careers`)
- [ ] Blog index + detail (`/blog`, `/blog/[slug]`)
- [ ] Legal pages (`/privacy`, `/legal`)

**Decisions and gaps from the Home build:**
- **~~Featured clients logo strip skipped entirely~~ — superseded.** Originally skipped (no real external client logos existed). Later rebuilt as `FeaturedProjects`, a split-panel showcase using Megagig's own real `CaseStudy` data and screenshots instead of third-party logos — confirmed via `WebFetch` against the reference site that its own "Featured Clients" section works the same way (the studio's own project names/screenshots, not external client logo marks). See the session log entries for the two build passes on this section.
- **`Lead.Create` intentionally still protected-only.** Making it public without Phase 6's rate-limiting/spam-mitigation would ship an unprotected write endpoint. `ContactBlock`'s `LeadForm` is fully built and will 401 on submit until Phase 6 wires the route.
- **`LeadForm`'s compact variant surfaced a real spec conflict, not yet resolved:** `project-requirements.md` §5.1 describes Home's contact form as "name, email, message" only, but the Go handler requires `phone`/`company`/`project_type`/`budget_range` (`binding:"required"`) on every `Lead.Create`, and `CreateLeadSchema` mirrors that. The compact form currently submits placeholder values ("Not provided" / "general" / "not_specified") for the fields it doesn't collect, purely so the payload shape is complete for whenever Phase 6 wires it up. **Phase 6 must decide**: loosen those required-field constraints for contact-sourced leads, or extend the compact form to collect them after all — don't ship the placeholder-value workaround into a real, functioning lead pipeline without deciding this properly.
- **`LeadForm` currently only implements the "compact" variant** (Home). Per `library-docs.md`'s "one LeadForm, three entry points" rule, `/start-project` and `/contact-us` (Phase 4.6/4.7) should extend this same component with a "full" variant (matching `CreateLeadSchema` exactly) rather than forking a second implementation — the select-option lists for `project_type`/`budget_range` weren't built now since they depend on the Services catalog slugs, which don't have a shared-constants home yet.
- **Home's ~15 new section components are verified via live SSR content assertions (curl against the running dev API with real seed data), not automated unit tests** — unlike Phase 3's primitives, these weren't given a `__tests__` suite in this pass, given the scope already covered in this session. Worth a `/review` pass before Phase 4 is considered fully closed out.
- **Tech stack strip and service-catalog icons are generic Lucide icons, not official brand marks** — no brand icon set is installed; flagged in `tech-stack.tsx`'s own comment.

**`CaseStudyPreview` `/recover` pass (2026-09-18) — found and fixed a critical, sitewide layout bug, not a one-section issue:**
- The user flagged that "Recent case studies" didn't match the reference site's "Recent Clients" section (wrong frame primitive, no page gutters, uneven middle gap). Diagnosed as Failure Mode 1 (isolated, clear root cause), but the root cause turned out to be **sitewide**: every section container's `px-[--space-container-x]` / `max-w-[--space-container-max]` / `py-[--space-section-y(-mobile)]` arbitrary-value syntax compiled to **invalid CSS** (`padding-inline:--space-container-x`, missing `var()`) under Tailwind v4.3.3 — v4 doesn't auto-wrap the bare `[--token]` bracket shorthand the way v3.4 did; it needs the `(--token)` parenthesis syntax instead. Confirmed via the actual compiled `.next` CSS output, not guesswork. This meant **every marketing section, plus Navbar and Footer** (17 files total) had zero horizontal page padding and no max-width constraint the whole time — content was silently full-bleed edge-to-edge, which is what made every alternating-row section look broken/uncentered no matter what was changed locally. Fixed by replacing `[--token]` with `(--token)` across all 17 files (mechanical, same fix each time); verified in the compiled CSS that `padding-inline: var(--space-container-x)` now emits correctly, confirmed live via curl against the running dev server.
- `CaseStudyPreview` itself: swapped `TabletFrame` (no chrome bar — meant for the hero device composition) for `BrowserFrame` (traffic lights + URL bar, matching the reference's actual browser-window look), added a new `bezel="thick"` variant to `BrowserFrame` (chunkier border/radius/chrome-bar padding, plus real dark "matting" padding around the screenshot inside the frame — the reference's screenshot doesn't touch the frame's edges, ours previously did), added a hover-lift + shadow transition. Reverted an intermediate fixed-width-image + flex-1-text layout (which looked unbalanced, with dead space in the flex-growing text column) back to an equal two-column `grid` — the reference actually uses two same-width columns with a small fixed gap and the image filling its whole column, not a narrower image floating in a wider one. Centered the "Real, shipped work / Recent case studies" section heading per explicit user request (the reference itself is left-aligned here — this is a deliberate, requested deviation, not a mismatch).
- `WhatWeBuilt` copy for all 7 seeded case studies expanded from one-line summaries to 5+ line paragraphs (still placeholder-but-honest marketing copy per this file's own documented precedent, not fabricated specifics) so the alternating-row layout reads as substantial as the reference's own paragraph-length descriptions, per explicit user request.
- **Found and fixed a real, previously-latent seeder bug while re-running seed to push the new copy live**: `FirstOrCreate(&existing, someStruct)` was passing the *entire* struct as extra query conditions (GORM's actual behavior for extra `conds` args), not just as create-time attributes — despite this file's own header comment claiming "existing rows are never touched." This was invisible as long as no seed field was ever edited after first creation; the moment `WhatWeBuilt` text changed, the WHERE clause implicitly required an exact match on the *new* text too, so it silently created a **duplicate row per case study** (14 rows for 7 slugs) plus a duplicate testimonial, instead of updating in place. Fixed all three loops (case studies, products, testimonials) to `.Where(...).Attrs(struct).FirstOrCreate(&existing)` — `.Attrs()` is create-only and never affects the match query, restoring the actually-intended "match by slug/company_name only, never touch existing rows" behavior. Cleaned up the duplicate rows by hand (verified via `docker exec psql`, checked `testimonials.case_study_id` was the only foreign reference before deleting); confirmed the fix by running `go run ./cmd/seed` twice in a row with zero duplicates created the second time.
- Added two new `Product` entries at the user's request: **SocietyLedger** (`https://www.societyledger.com.ng`, reusing its existing, already-confirmed `CaseStudy` copy) and **MedSafe** (`https://medsafe.com.ng`) — MedSafe's tagline/description/feature bullets are grounded in a live `WebFetch` of its real site (drug monographs, interaction checker, NAFDAC verification, clinical calculators), not invented, same anti-fabrication approach used everywhere else in this file. Neither has a real screenshot yet (`lib/product-screenshots.ts` has no entry for `medsafe`), so `ProductsSection` correctly renders MedSafe's card without a screenshot block — matches the existing, established pattern.
- Re-verified end-to-end after all of the above: `tsc --noEmit`, `next build`, and the full 35-test suite all clean; live-curl-confirmed against the running dev server (new copy present, new grid classes present, `padding-inline: var(...)` correctly compiled).
- **Real contact details set** (first piece of Phase 5 content, done ahead of schedule at the user's request): `SiteSettings.contact_email`/`contact_phone`/`whats_app_number` updated from placeholders to the founder's real values. Since `SeedSiteSettings` is a singleton guarded by `count > 0` (not the `FirstOrCreate` pattern fixed above), editing the seeder source alone wouldn't touch the already-seeded row — updated both: the seeder source (for future fresh databases) and the live dev-DB row directly (`docker exec psql`). Confirmed no other file hardcodes the old placeholder values (`hello@megagig-software-solution.dev`, `+234 000 000 0000`) outside test fixtures — every real usage (Footer, WhatsAppFab, ContactBlock, QuoteCtaBand) reads from this one `SiteSettings` fetch in `(marketing)/layout.tsx`, so the single DB update propagated everywhere automatically. Verified live via curl once the `revalidate: 60` ISR window passed.

## Phase 5 — Real content population
- [ ] PharmacyCopilot case study written + published
- [ ] BusinessCopilot case study written + published
- [ ] PharmacyCopilot + BusinessCopilot Product entries created
- [~] SiteSettings filled with real contact details (placeholders removed) — real `contact_email`/`contact_phone`/`whats_app_number` set (2026-09-18, see Phase 4 notes); hero headline/subhead, address, and social links are still placeholder
- [ ] Initial FAQ set published
- [ ] Team member(s) added
- [ ] Careers/Blog empty-states verified to look intentional

## Phase 6 — Lead pipeline wiring
- [ ] `lead-notify.html` + `lead-confirm.html` email templates built
- [ ] All three lead entry points wired to `POST /api/v3/leads` with correct `source`
- [ ] Rate limiting on the public leads route
- [ ] Basic spam mitigation (honeypot or equivalent)
- [ ] Full loop manually verified (submit → DB row → 2 emails → visible + status-editable in admin)

## Phase 7 — SEO, performance, accessibility pass
- [ ] Per-page metadata across all public routes
- [ ] `sitemap.xml` + `robots.txt` (web: allow; admin: disallow)
- [ ] Image optimization audit
- [ ] Lighthouse pass (Home, one case study, one product, Pricing) — all green
- [ ] Keyboard/screen-reader pass (navbar, forms, accordion, carousel)

## Phase 8 — Deployment
- [ ] Production Postgres/Redis/S3-compatible storage stood up
- [ ] `apps/api` deployed
- [ ] `apps/web` deployed to Vercel
- [ ] `apps/admin` deployed to Vercel (own subdomain)
- [ ] DNS + SSL verified on all hosts
- [ ] Production lead pipeline smoke-tested (real Resend delivery)

## Phase 9 — Post-launch backlog (not started; tracked, not built)
- [ ] File attachment on quote form
- [ ] Case-study filter chips
- [ ] Multi-role admin (editor vs admin)
- [ ] Blog scheduled publishing
- [ ] `/brand` page with logo kit
- [ ] Analytics integration
- [ ] i18n / multi-language

---

## Session log

> One line per work session — what shipped, what's next. Newest entry on top. Keep entries short; detail belongs in commit messages, not here.

- 2026-09-18: `/recover`'d `CaseStudyPreview` after the user compared it directly against the reference site's "Recent Clients" section. Root cause turned out sitewide, not local: every section's `px-[--space-container-x]`-style arbitrary-value token was invalid CSS under Tailwind v4 (missing `var()` — v4 needs `(--token)` parens, not `[--token]` brackets), so the whole site had zero page padding/max-width this whole time. Fixed across all 17 affected files. Also swapped `TabletFrame` for a new `BrowserFrame` `bezel="thick"` variant (chrome bar + dark matting around the screenshot), rebalanced the row back to an equal two-column grid, centered the section heading, expanded all 7 case studies' `WhatWeBuilt` copy to 5+ lines, and fixed a real latent GORM seeder bug (`FirstOrCreate` was matching on the whole struct, not just slug, so editing seed text silently duplicated rows instead of updating them) discovered while pushing the new copy live. Added `SocietyLedger` and `MedSafe` as new `Product` entries per the user's request (MedSafe's copy grounded in a live `WebFetch` of its real site). Full details in the Phase 4 notes above. `tsc`/`next build`/35-test suite all clean; `go build` clean; reseeded twice with zero duplicates.
- 2026-09-17: Four quick follow-up fixes in one pass, all user-flagged live against the running dev site (ports 3000/8080 — the BusinessCopilot port collision noted below had since cleared): (1) `/recover`'d `FeaturedProjects` wrapping the whole section in a hardcoded dark background + inverted heading instead of only the inner card's left panel — reverted to the normal theme background/heading; (2) added 5 real Megagig products as proper `CaseStudy` seed rows so "Featured clients"/"Recent case studies" have real content to show, per the founder's list, skipping the ones with no real screenshot rather than fabricating one; (3) made the FeaturedProjects name-pill list actually clickable (added a `Carousel` `index` prop) after the user pointed out they shouldn't have to wait for auto-rotation — reverses an earlier "display-only pills" decision, now covered by a dedicated Carousel test; (4) rebuilt `CaseStudyPreview` from a card grid into an alternating image/text row list (and removed Hero's "Building since 2023 · Lagos, Nigeria" line) to match the reference site's actual "Recent Clients" layout, which is structurally different from the card-grid `SelectedWork` section further down the page. All re-verified live; `tsc`/`next build`/35-test suite all clean.
- 2026-09-17: User flagged the just-rebuilt `FeaturedProjects` as still wrong — "dark background left and right, clustered and unprofessional," with a side-by-side screenshot. Root cause: the peek devices were sized at 70% of the panel width with no explicit panel height, so they were nearly the same size as the main frame and collided with it, and the caption overlay's oversized gradient covered nearly half the image. Fixed with concrete fixed-width peeks (`w-40`/`w-48` instead of a percentage), an explicit `md:min-h-[460px]` on the split card, and a much more compact caption overlay. **Environmental discovery, not a code bug:** while verifying, found that ports 3000 and 8080 were both being served by a completely unrelated project (`BUSINESSCOPILOT`, also Grit-scaffolded, same default ports) that the user had running separately — not a leftover from this session. Verified this fix on alternate ports (web :3005, API :8090 via `APP_PORT`) rather than touching that other project's processes. **Note for future sessions:** if `localhost:3000`/`:8080` ever serve unexpected/stale-looking content (e.g. old removed attributes, 404s on routes known to exist), check for a BusinessCopilot dev server before assuming this project's code is broken — verify on an alternate port instead of killing what's already there.
- 2026-09-17: User flagged `FeaturedProjects` (the "featured clients" section) as underbuilt vs. the reference (desishub.com), with an image. Ran `/recover` — Failure Mode 1 (isolated section, first complaint, clear gap), not a repeat of the earlier whole-page redesign issue: the "reuse Carousel not a tab-selector" decision from before only covered the click-to-swap *interaction*, not the surrounding visual composition, which was never actually built (bare single frame, plain caption text below it, no split panel, no peek devices). Fetched the live reference site (`WebFetch`) to confirm exact behavior before rebuilding: the preview image is itself a link to that project's case-study page. Rebuilt `FeaturedProjects` as a split dark/light panel (display-only name-pill list synced to the active slide via a new `Carousel` `onIndexChange` prop, `Carousel` dots hidden but arrow controls kept per ui-rules.md §9) with the caption overlaid on the screenshot via a gradient scrim, each slide now linking to `/case-study/[slug]`, and two dimmed peek devices behind the active slide for depth. Re-verified live (links/pills/overlay content all confirmed present in rendered HTML); `tsc`/`next build`/34-test suite all clean.
- 2026-09-17: User added 3 new real screenshots to `apps/web/public/` (SocietyLedger dashboard + landing page, Yazzy OS) and asked for thicker, tablet-like bezels on `HeroShowcase` (matching the reference image more exactly) plus all 5 real screenshots in the composition, dropping the accidental duplicate PharmacyCopilot image (PC + POS were both in use). Built `TabletFrame` (thick bezel, no chrome bar — distinct from `BrowserFrame`'s thinner treatment used elsewhere) and reworked `HeroShowcase` into a 5-device fanned composition. Re-verified live (all 5 images confirmed present, duplicate confirmed gone); `tsc`/`next build`/34-test suite all clean.
- 2026-09-17: User provided a reference image and asked for a tilted, overlapping multi-device screenshot composition directly under the hero CTAs. Built `HeroShowcase` (3 real screenshots, rotated/overlapping, desktop-only) and removed the now-redundant `ShowcaseStrip` carousel it replaced. Re-verified live; all clean. Next: `/review` Home, then continue Phase 4 page by page.
- 2026-09-17: User flagged the first Home build as visually flat/generic vs. the reference site (desishub.com), with screenshots. Ran `/recover` — diagnosed as Failure Mode 3 (the token/rule-compliant approach itself was the problem, not a patchable bug) plus one isolated Failure Mode 1 bug (overlapping category-tag badges). Ran `/architect` again for the redesign specifically, then fixed everything: real `BrowserFrame`-wrapped screenshots (new primitive) replacing letter placeholders, a bigger/bolder Hero with a restrained radial-glow background (updated `ui-rules.md` §1 to explicitly permit this, rather than silently drifting from the "no gradients" rule), a new `FeaturedProjects` dark-panel section reusing `Carousel`, and one solid-accent highlighted card per grid (Services, Pricing). Re-verified live; all clean. Next: `/review` Home (now redesigned), then continue Phase 4 page by page.
- 2026-09-17: Ran `/architect` for Phase 4, which surfaced a hard blocker before any frontend work could start: CaseStudy/Product/Testimonial/FAQ had no public, unauthenticated routes at all (only Blog did) — added them under a new `/api/v1/public/*` prefix (Blog's own bare-path trick doesn't transfer since the bare paths are already taken by protected CRUD for these four). Then built Home (`/`) end-to-end — all ~14 sections from `project-requirements.md` §5.1, replacing 100% Grit demo content, composed entirely from Phase 3 primitives. Got real founder bio/photo and real trust-stat figures directly from the user rather than fabricating placeholder content for the static sections `ui-rules.md` requires to be truthful. Skipped the "featured clients" logo strip entirely (no real client logos exist yet — same anti-fabrication reasoning). `Lead.Create` stays protected-only by design (Phase 6 owns making it safely public); `LeadForm`'s compact variant is built and wired but will 401 until then, and surfaced a real, disclosed spec conflict between the "compact form" wording in requirements and the Lead model's required fields for Phase 6 to resolve. Verified live against the running API with real seed data; `tsc`/`next build`/the 34-test suite all clean. Next: `/review` this Home build, then continue Phase 4 page by page (Services next, per build-plan.md's order), each getting its own `/architect` pass.
- 2026-09-17: Ran `/review` against Phase 3, then fixed everything it found. **[Critical]** `lib/site-settings.ts`'s fetch had no revalidation strategy and the code comment claiming "Next respects the origin's Cache-Control header" was factually wrong — Next's Data Cache only honors `fetch()`'s own `cache`/`next.revalidate` options. Confirmed via `next build` output (`○ Static` for `/` and `/blog`, no revalidate) that `SiteSettings` was being frozen at build time, meaning admin edits to the WhatsApp number/contact info would never appear without a redeploy — directly contradicting `library-docs.md`'s revalidate convention and `project-requirements.md` §6. Fixed with `next: { revalidate: 60 }`; rebuilt and confirmed the build output now shows `Revalidate: 1m`. **[Important]** Discovered the review flagged that most new primitives were never exercised at runtime — while addressing this, also found the pre-existing `__tests__/navbar.test.tsx`/`footer.test.tsx` were fake stubs testing locally-defined mock components, not the real ones (one assertion was already failing independent of anything this session touched). Rewrote both against the real components and added a real test file for every other Phase 3 component (Button, Card, Badge, SectionHeading, Accordion, Carousel, StatCallout, TechIcon, ThemeToggle, WhatsAppFab, BackToTop) — 13 files, 34 tests. This caught a genuine bug: `Carousel` was typed `children: ReactNode[]` and called `.map()` directly, which throws for exactly one slide (React only array-wraps 2+ JSX children, a single child arrives bare) — fixed with `Children.toArray()`. **[Minor]** Navbar's scroll-based background state initialized to `false` and only corrected in a post-mount `useEffect`, causing a one-frame flash of the transparent/top-of-page style when Home is reloaded already scrolled down — fixed by switching to an isomorphic `useLayoutEffect` (runs before paint client-side, falls back to `useEffect` during actual server rendering to avoid the SSR warning). All 34 tests pass, `tsc --noEmit` and `next build` both clean, re-verified live against the running API.
- 2026-09-17: Closed out Phase 3 — public site global chrome. Retrofitted `apps/web` into a literal `(marketing)` route group (architecture.md §4), deleted the unused `(auth)` customer-account scaffold and the old pathname-based `AppChrome.tsx`, and rebuilt Navbar/Footer from scratch (previous versions were unmigrated Grit demo branding using CSS classes that don't exist in `ui-tokens.md`). Built 8 shared UI primitives (Button/Card/Badge/SectionHeading/Accordion/Carousel/StatCallout/TechIcon) hand-rolled with CVA rather than the shadcn CLI `library-docs.md` names — the token vocabulary mismatch made the CLI not worth running; installed `@radix-ui/react-accordion` as the one real new dependency. Built `WhatsAppFab` and `ThemeToggle` (mirrors admin's `DarkModeToggle`). Verified live end-to-end against the running dev API with real seeded `SiteSettings` data; `tsc`/`next build` both clean. Logged full class-level detail in `ui-registry.md` via `/imprint`. Next: Phase 4 — public site pages, starting with Home (currently still Grit demo placeholder content) once the primitives above are consumed page by page.
- 2026-09-17: Rebranded the admin login/auth pages — the `(auth)` theme engine (`packages/shared/themes.ts`, separate from `tokens.css`/ui-tokens.md and explicitly out of scope until now) had Grit's default indigo (`#4f46e5`) for the hero panel and social-button accent. Changed `atlas` theme's `accent`/`heroBg` to match Megagig's actual brand tokens (`--color-accent` green, `--color-brand` blue) — `primary` already matched by coincidence. Replaced the "Built with Grit — Go + React framework" footer line with a dynamic copyright (`© {year} {brand.name} Ltd.`, using `brand.config.ts`'s name so a future rebrand stays in sync), and fixed the admin app's leftover Grit meta title/description. Verified live: hero panel now renders `#2563eb`, no more Grit branding text anywhere on the login page.

- 2026-09-17: Ran `/review` against Phase 1 + Phase 2 before starting Phase 3. Found 4 real issues by reading actual handler code and testing live against the running API rather than trusting prior smoke tests: `CaseStudy.testimonial_id` was completely dead (never bound in Create/Update/Patch, never Preloaded — the Phase 1 "follow-up field" claim was wrong), `Testimonial.Create` still hard-required `case_study_id`/`avatar_id` despite being designed optional, `Blog.Author` was never Preloaded anywhere, and `--color-status-*` had no dark-mode values. Fixed all 4, verified each live (created/linked/unlinked/deleted real test records via curl), restored demo data afterward. Next: Phase 3 — public site global chrome (navbar/footer, WhatsApp FAB, shared UI primitives, theme toggle).
- 2026-09-17: Closed out Phase 2. Added Megagig-specific dashboard stats (new leads this week, published case studies/products) alongside the pre-existing generic per-resource widgets; built Lead status badge coloring + status filter (table + form, upgraded from free-text to select); added `apps/admin/app/robots.ts` (disallow-all). Fixed two real backend gaps found while wiring this up: `CaseStudy`/`Product` list handlers didn't support `?published=true` (their admin table's own Published filter was silently non-functional), and `Lead` had no status filter at all — both fixed via the existing `paginate` package's `.With()` mechanism, verified live against the running API (created/filtered/deleted a test lead). Corrected a wrong Phase 1 note: generated handlers do Preload correctly (`paginate.List[T]` pattern), the earlier claim was based on inspecting dead/unused service files. Confirmed admin auth-gating and robots.txt were the only structural asks; auth-gating was already solid pre-existing. Next: Phase 3 — public site global chrome (navbar/footer, WhatsApp FAB, shared UI primitives, theme toggle).
- 2026-09-17: Closed out Phase 1. Generated TeamMember, CaseStudy, Testimonial, Product, JobOpening, FAQ, Lead via `grit generate`; extended the existing `Blog` resource into the full `BlogPost` field set instead of duplicating it; hand-built the `SiteSettings` singleton (model/service/handler/routes/admin page). Fixed several real bugs surfaced during verification: a broken `.name` display field on every Upload relationship (should be `original_name`), a missing tag-input field type (string_array fields were rendering as image-upload dropzones), and a GORM AutoMigrate failure from the two-way CaseStudy↔Testimonial relation (fixed via `DisableForeignKeyConstraintWhenMigrating`). Seeded placeholder SiteSettings + demo CaseStudy/Product/Testimonial rows. All builds/type-checks clean; all endpoints and admin pages smoke-tested. Next: Phase 2 — admin panel polish (dashboard widgets, Lead status badges/filter, auth-gating confirmation, subdomain + robots.txt).
- 2026-09-16: Closed out Phase 0. Verified Docker infra healthy (Postgres/Redis/MinIO/Mailhog), confirmed the auth model matches `architecture.md` §6 (kept the full Grit enterprise module set dormant/untouched per explicit no-delete-without-approval decision), fixed a font-wiring bug (Inter was bound to an unused `--font-display` var instead of `--font-sans`, so it never actually rendered) in both `apps/web` and `apps/admin` layouts, and confirmed all three dev servers + GORM Studio respond 200. Next: start Phase 1 — generate the core content resources (TeamMember, Testimonial, CaseStudy, Product, JobOpening, FAQ, BlogPost, Lead, SiteSettings).
- 2026-09-16: Wired `ui-tokens.md` into `packages/shared/themes/tokens.css` (Tailwind v4 CSS-first), upgraded `apps/web` + `apps/admin` to Tailwind v4, and renamed admin's entire legacy multi-theme class system (98 files) to match. Both dev servers smoke-tested (200 OK, correct tokens in compiled CSS, no console errors). Next: finish remaining Phase 0 items (docker services, fonts decision, API dev server) before starting Phase 1 resources.
